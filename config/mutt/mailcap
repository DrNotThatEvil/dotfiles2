text/html; mv %s %s.html && qutebrowser %s.html; test=test -n "$DISPLAY"; edit=nvim; compose=nvim; nametemplate=%s.html

text/html; w3m -I %{charset} -T text/html; copiousoutput;

# text/html; mv %s %s.html && /usr/bin/qutebrowser %s.html > /dev/null 2>/dev/null; needsterminal;

image/*; ~/.config/mutt/muttimage.sh %s ; copiousoutput

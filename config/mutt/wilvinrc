set sendmail = "/usr/bin/msmtp -a wilvin"

source "~/.config/mutt/mailboxes"

set realname = "Willmar Knikker"
set from="Willmar Knikker <wilvin@wilv.in>"
set folder = "~/Mail"
set envelope_from
set mail_check = 0

unset move           # gmail does that
set delete           # don't ask, just do
unset confirmappend  # don't ask, just do!
set quit             # don't ask, just do!!

set ssl_force_tls = yes
set spoolfile = "+wilvin/INBOX"
set record = "+wilvin/Sent"
set postponed = "+wilvin/Drafts"
set sort = threads
unset message_cachedir
# set message_cachedir = ~/.mutt/messagecache
set imap_check_subscribed
set imap_keepalive = 300
set ssl_starttls =yes

set fcc_attach
unset mime_forward
set forward_format = "Fwd: %s"
set include
set forward_quote

ignore *                               # first, ignore all headers
unignore from: to: cc: date: subject:  # then, show only these
hdr_order from: to: cc: date: subject: # and in this order

macro index gi "<change-folder>=INBOX<enter>" "Go to inbox"
macro index gs "<change-folder>=Sent<enter>" "Go to sent"
macro index gd "<change-folder>=Drafts<enter>" "Go to drafts"
macro index gj "<change-folder>=Junk<enter>" "Go to junk"
macro index gt "<change-folder>=Trash<enter>" "Go to trash"
